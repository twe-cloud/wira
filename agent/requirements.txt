# Pinned exactly: the Windows build verifies the matching native DLL by SHA-256
# (see .github/workflows/build-windows.yml). Bumping this version requires
# updating the expected hash there too.
neonize==0.3.18.post0
httpx>=0.27.0
openai>=1.50.0
anthropic>=0.40.0
ollama>=0.4.0
qrcode>=8.0
Pillow>=10.0.0
python-magic>=0.4.27; platform_system != "Windows"
python-magic-bin==0.4.14; platform_system == "Windows"
pyinstaller>=6.0
