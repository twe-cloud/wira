
agent/build/
agent/dist/
agent/.venv/
agent/*.sqlite3
.video-work/
site/public/video/

# Local Netlify folder
.netlify

# Local-only signing/ship tooling (embeds keychain password + signing key paths).
# Never publish to the public repo.
agent/scripts/rebuild-and-resubmit.sh
agent/scripts/ship-direct-download.sh
